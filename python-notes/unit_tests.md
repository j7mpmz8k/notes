
failure(F) -> test crashes
error(E) -> test worked but failed the code

```python

class TestRotate(): #inherits from unittest.TestCase
    def test_lowercase(self):#needs to start with `test` so unit test can find method
        self.assertEqual()

if __name__ = __main__:
    #run unit test
```
